# ESM Sports Network — https://esm-sports-network.vercel.app
User-agent: *
Allow: /

# Private app routes — nothing to index (auth-gated single-page apps).
Disallow: /admin/
Disallow: /portal/
Disallow: /scout/
Disallow: /register/
Disallow: /tenerife/

# /roster/ and /players/*.html are intentionally noindex (gated roster + detail).
# They are left crawlable on purpose so crawlers can SEE the noindex directive and
# keep them out of the index cleanly — do NOT Disallow them here.

Sitemap: https://esm-sports-network.vercel.app/sitemap.xml
